package com.unios.repository;

import com.unios.model.CareerOutcome;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CareerOutcomeRepository extends JpaRepository<CareerOutcome, Long> {
}
